spring:
  application:
    name: AccountService

  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/transaction
    username: user
    password: user
    driver-class-name: org.postgresql.Driver